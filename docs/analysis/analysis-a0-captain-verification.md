# Captain 数据核验记录 — 权威口径基线

> **核验人**: captain（fly64-logs-deep-analysis）  
> **日期**: 2026-09-22  
> **目的**: 三位分析师对"分析范围"存在分歧，本文档用原始数据裁定，供 A4 综合报告采用  
> **核验脚本**: `scripts/_captain_verify_counts.py`、`scripts/_captain_verify_topics.py`、`scripts/_captain_verify_msgs.py`

---

## 1. 裁定：权威口径为 **11 个唯一 session / 874 条真实用户提问**

### 1.1 数据源裁定

| 数据源 | 状态 | 裁定 |
|--------|------|------|
| `export logs/_extracted_38542b1c` | **0 文件**（陈旧残留） | ❌ 不可用 |
| `export logs/_extracted_f953d3fd` | **0 文件**（陈旧残留） | ❌ 不可用 |
| **`.tmp/sessions_v3/`** | 16 个 zip 全量重新解包，354 MB | ✅ **权威数据源** |

> rootcause-analyst 的「仅 9 session / 342 提问」结论源自读取了前两个空目录，**不成立**。

### 1.2 逐 session 真实提问数（已剔除 97 条协议噪声）

| Session | v3 原值(all) | **权威(real)** | 噪声 | canonical 目录 |
|---------|------------:|--------------:|-----:|---------------|
| 1f8fbe04 | 94 | **80** | 14 | `1f8fbe04-52eb-4dc8-9b63-b0f497c48b23` |
| 27ed0979 | 51 | **51** | 0 | `27ed0979-15ec-4711-bd47-c8cf63b109b8` |
| 38542b1c | 209 | **198** | 11 | `38542b1c-45e7-4c96-b615-94875cd2bd0e` |
| 71c21f6d | 7 | **4** | 3 | `71c21f6d-…-822765a043e4 (1)` |
| 90dd512b | 64 | **64** | 0 | `90dd512b-6955-4848-9216-3c3c94c35b41` |
| 99cab60f | 40 | **35** | 5 | `99cab60f-…-ababdd0f4373 (3)` |
| b2eeed98 | 59 | **63** | 0 | `b2eeed98-…-058bde87f475 (1)` ← **须用 (1)** |
| d983cef5 | 66 | **66** | 0 | `d983cef5-3f9c-4b97-9eac-820390a38bbd` |
| ed4b8026 | 3 | **3** | 0 | `ed4b8026-6bfe-40ee-9be8-b5fb3b59e612` |
| f953d3fd | 356 | **300** | 56 | `f953d3fd-dcea-430f-9bee-461b8d73943e` |
| fdb47617 | 10 | **10** | 0 | `fdb47617-7c9a-46b3-8cc0-80588409602c` |
| **合计** | **963** | **874** | **89** | |

**结论**：`874` 与 theme-analyst、report-synthesizer 的独立复算**三方一致**。

**计算链（已修正表末行的基数笔误）**：

```
canonical 11 session 全量 kind=user  = 963   ← S7 用完整 (1) 导出
减 协议噪声（派发提示词 + 入队通知） =  89
= 真实用户提问                        = 874
```

> **rev 2 自查修正**：本表初版末行写「959 / 874 / 97」，两处笔误——`959` 是 v3 使用 legacy S7 导出的旧基数，`97` 是把 16 个重导出目录的未去重噪声数误抄进来。逐行数据（噪声 14+0+11+3+0+5+0+0+0+56+0）**恰为 89**，与 `963 − 874 = 89` 自洽。A2 rev3 与 A4 均独立复算出 963/89/874，故以 **874** 为准。
>
> 附：若改用 legacy S7 导出（59 条，**不完整**），则基数为 959、真实提问为 870 —— 这正是 A2 报出 870 的原因。§1.3 证明 (1) 是 legacy 的**严格超集**，故 870 应废弃。

**v3 与权威的净差额**：`963 − 874 = 89` 条协议噪声；v3 自身报 959 另因 S7 少算 4 条 → 合计与权威差 `89 + 4 = 93` 条口径。

### 1.3 S7 导出选择（超集判定，已实测）

| 导出 | kind=user | 去重后 | 裁定 |
|------|---------:|------:|------|
| `b2eeed98-…` (legacy, 16.85 MB) | 59 | 58 | ❌ v3 误用（不完整） |
| **`b2eeed98-… (1)`** (v3, 13.3 MB) | **63** | **62** | ✅ 采用 |

**超集证明**（`scripts/_captain_resolve_s7.py`）：

```
only in legacy : 0     ← (1) 未丢失 legacy 的任何一条
only in (1)    : 4     ← (1) 独有 4 条
union unique   : 62
```

`(1)` 独有的 4 条正是 **09-21 探索三重死锁**现场提问：

1. 「分析记录的逻辑访问数据，分析脑模型为什么没有实现路径访问的探索能力，始终在已经走过的路径往返重复走」
2. 「/agent-teams ## 根因分析：脑模型为什么无法激活探索能力 … 核心发现：三重死锁」
3. 「更新脑模型版本，重启脑模型」
4. 「脑模型已经重启，评估是否问题可以解决 … cx_novelty_dir 仍在 0.0 的根因是区域 cells 全面陈腐（1524 次/格）…」

> 这 4 条是**探索三重死锁**这一关键战役的原始记录，v3 因使用 legacy 导出而完全遗漏 —— 佐证必须采用 (1)。

---

## 2. 裁定：v3 报告的"话题热度"表被协议噪声污染

| 话题 | v3 原值 | **权威(real)** | 膨胀倍数 |
|------|-------:|--------------:|---------:|
| AgentTeams（团队/编排） | 520 | **68** | **7.6×** |
| Bridge/启动/SM64 | 504 | **393** | 1.3× |
| EVO 进化 | 309 | **282** | 1.1× |
| 探索/逃离/覆盖 | 260 | **253** | 1.03× |
| 视觉系统 | 250 | **221** | 1.13× |
| 卡死/循环/运动质量 | 216 | **216** | 1.0× |
| 仪表板/UI/轨迹 | 214 | **201** | 1.06× |
| Coach/教官 | 134 | **129** | 1.04× |

**关键修正**：
1. **AgentTeams 不是最大顽疾**。真实命中 68，排名**末位**；v3 的"519/520 榜首"87% 是 `You have joined the team` 入队通知与自动派发提示词。
2. **真正的第一大话题是「运行环境/桥接/SM64 启动」**（393 命中）。
3. 真实"任务未执行"抱怨仅 **7 条**（rootcause-analyst 报 5 条，口径略窄），集中在 09-16 → 09-20，之后消失。

---

## 3. 裁定：rootcause-analyst 的三处"口径校正"全部不成立

| # | A2 的主张 | 核验结果 | 证据 |
|---|----------|---------|------|
| 1 | Coach 抱怨链**止于 09-18** | ❌ **错**。最后一条 coach 相关人话在 **09-22 18:12** | session 99cab60f：`修复 _inject/读取方的键名归一化… Coach 与 EVO 写同一文件会产生互相覆盖` |
| 2 | 「大部分动作没尝试」零命中 | ❌ **错**。含「有」的原句确实存在，且是**用户原话**（**2 条**） | 见下方精确分类 |
| 3 | 仅 9 session / 342 提问 | ❌ **错** | 见 §1.1：读了空目录 |

> v3 报告把原话转述为「大部分动作没尝试」（漏「有」字）属措辞不精确；**原句确实存在**。

**精确分类（rev 3 修正，脚本 `scripts/_captain_verify_phrase.py`）**：

| 变体 | 用户原话 | 全部原始命中 |
|------|---------:|------------:|
| `大部分动作没有尝试`（精确句） | **1**（`99cab60f` seq=2506） | 3（+assistant +inbox-spliced） |
| `大部分没有尝试`（短变体） | **1**（`99cab60f` seq=2463） | 3（+assistant +inbox-spliced） |
| **合计** | **2 条用户原话** | 6 处原始事件 |

> **rev 3 自查**：本表初版写「3 处命中」，是**原始事件数**（含 `assistant/message` 与 `agent/inbox/spliced` 重投递），不是用户提问数。rootcause-analyst 备案指出后我复算确认：真实用户原话为 **2 条**（两个变体各 1 条）。同时将所有相关 session 定位从 09-22 17:18 更正为 **09-22 17:09**（`seq=2463` 为首次提出）。
>
> 结论不受影响：原句确实是用户原话，A2 rev 1 的"零命中、系二次引用"判定仍为错误。

---

## 4. 裁定：theme-analyst 的三条修正全部成立

| # | A1 的主张 | 核验结果 |
|---|----------|---------|
| 1 | 真人提问 874（非 959） | ✅ **确认**（§1.2） |
| 2 | AgentTeams 519 中 87% 是协议噪声 | ✅ **确认**（§2，实测 520→68） |
| 3 | v3「时间跨度」列是导出批次 touch 时间 | ✅ **合理**：6 个 session 末事件挤在 09-20 18:18–18:20 |

---

## 5. 给 A4（综合报告）的强制要求

1. **采用 874 / 11 session 口径**，并在报告开头显式声明；不得沿用 v3 的 959。
2. **不得引用**"AgentTeams 是最大顽疾/519 次"——改用"真实任务未执行抱怨 7 条"。
3. 话题排名以 §2 的 real 列为准，「运行环境/桥接/SM64」为第一大主题。
4. Coach 问题的时间线须写到 **09-22 18:12**。
5. 引用原话须使用原始措辞（如「大部分动作**有**尝试」）。
6. 在 v4 中**显式列出对 v3 的修正清单**（本文件 §1–§4 即为该清单）。

---

## 6. 裁定：成员对本文件的质询与收口（rev 2）

A2 rev3 与 A4 均独立复算并**挑战了本文件的算术**，这是正确的做法。逐条裁定：

### 6.1 「963 未去重 / 应为 870」—— 不成立，874 成立

| 主张 | 裁定 |
|------|------|
| 963 是"未去重的 16 目录计数" | ❌ **错**。963 是 captain 在 **11 个 canonical 目录**内实测的 kind=user 数，仅 S7 选用不同的导出 |
| 应改为 870（959 − 89） | ❌ **不成立**。870 的前提是把 **不完整的 legacy S7 导出**（59 条）当基线；§1.3 已证明 `(1)` 是其**严格超集**（only in legacy = 0），正确基数应为 63 |

**最终口径：`874`（不变）。** 修正的是本文件初版**表末行的两处笔误**（959 → 963、97 → 89），不是结论。

### 6.2 「协议噪声 89 还是 97 / 96」—— 权威值 89

- 逐行噪声：14+0+11+3+0+5+0+0+0+56+0 = **89** ✅
- `963 − 874 = 89` ✅ 自洽
- 初版末行"97"为误抄（来自 16 目录未去重口径），captain 邮件中"96"为笔误 —— **均以 89 为准**。

**采纳 A4 的差额归因表述（比 captain 初稿更精确）**：

```
v3 报 959  −  权威 874  =  85 条净差额
拆解：85 = 89（v3 误计的协议噪声） − 4（v3 因用 legacy S7 而少算的 4 条）
验证：874 − 4 + 89 = 959 ✓
```

> captain 初稿曾写"净差额 89 或 93"，不够准确 —— **85 才是 v3 与权威的净差额**，89 是其中被误计的噪声量，4 是另一处方向的偏差。以 A4 表述为准。

### 6.3 Coach 消息数 73（A2）vs 75（A1）—— 采纳 73 为主口径

- **主口径 73**：A2 在**协议过滤后语料**上按 human 消息逐条核对，并已排除 2 条恰好含 coach 关键词的派发消息（更严格）。
- **参考值 75**：A1 在 874 语料上按关键词复算，未做"是否为人话"的二次判定。
- **裁定**：对外主用 **73**，可括注 75。二者差异不影响任何结论；A4 已按"73（主）/75（A1 语料）"双写，**认可**。

### 6.4 A3 范围声明 —— 认可

state-analyst 声明 A3 仅覆盖代码/参数/测试/版本状态、不涉及 session 计数口径，因此不采纳也不传播 —— **正确且专业**，予以认可。

---

## 7. 最终一致性检查表

| 产物 | 应含口径 | 状态 |
|------|---------|------|
| `analysis-a0-captain-verification.md`（本文） | 874 / all=963 / noise=89 | ✅ rev 2 已修正笔误 |
| `analysis-a1-themes.md` | 874 / 11 session | ✅ |
| `analysis-a2-rootcauses.md`（rev 3） | 874（§0 已对齐） | ✅ |
| `analysis-a3-state.md` | 不涉 session 计数 | ✅ 合规 |
| `session_logs_analysis_v4.md` | 874 / noise=89 | ✅ |
| `session_logs_execution_plan_v4.md` | 874 | ✅ |

**五份产物口径现已完全一致：11 session / 874 真实用户提问 / 协议噪声 89 条。**

---

> **生成**: captain · 2026-09-22（rev 2：修正 §1.2 表末行基数笔误，新增 §6 质询裁定与 §7 一致性检查）  
> **保存位置**: `docs/analysis/analysis-a0-captain-verification.md`
