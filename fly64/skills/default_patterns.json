{
  "$schema_version": "2.0",
  "patterns": [
    {
      "id": "circle_loop",
      "name": "Circle loop — 无障碍转圈",
      "version": "1.0.0",
      "description": "Mario circles in place on flat ground, cliff avoidance blocking forward movement.",
      "conditions": {
        "wall_score": {"max": 0.1},
        "asymmetry_magnitude": {"max": 0.06},
        "stuck_duration": {"min": 120},
        "ground_angle": {"min": 0.3}
      },
      "contradiction": "terrain=cliff but ground_angle>0.3 (not a cliff)",
      "diagnosis": "Terrain classifier false-positive 'cliff'; cliff avoidance overrides straight-forward escape.",
      "fix_template": "# Fix: Add ground_angle gate before cliff avoidance\n# File: fly64/fly64/main.py\n# Find: {cliff_trigger_line}\n# Replace: {cliff_trigger_replace}\n",
      "fix_files": ["fly64/fly64/main.py"],
      "severity": "high",
      "tags": ["navigation", "terrain", "cliff"],
      "rollback_strategy": "revert_line",
      "threshold_justification": "wall_score<0.1=flat terrain, asymmetry<0.06=symmetrical movement (tuned from 0.05 based on real measured values), stuck>120s=abnormal duration, ground_angle>0.3=not a cliff edge"
    },
    {
      "id": "ramp_trap",
      "name": "Ramp trap — 斜坡上无法脱困",
      "version": "1.0.0",
      "description": "Mario stuck on a ramp, suppression blocks turning.",
      "conditions": {
        "ramp_score": {"min": 0.5},
        "stuck_duration": {"min": 180},
        "position_unchanged_60s": true
      },
      "diagnosis": "Ramp suppression blocks turning; Mario stays on slope.",
      "fix_template": "# Fix: Add ramp escape override when stuck >180s on slope\n# File: fly64/fly64/main.py\n# Add after slope detection:\nif ramp_score > 0.5 and stuck_duration > 180:\n    control.x = rng.integers(60, 80) * (-1 if rng.random() < 0.5 else 1)\n    control.y = 40\n",
      "fix_files": ["fly64/fly64/main.py"],
      "severity": "high",
      "tags": ["navigation", "terrain", "ramp"],
      "rollback_strategy": "revert_added_block",
      "threshold_justification": "ramp_score>0.5=strong slope signal, stuck>180s=3min threshold, position_unchanged=no progress despite effort"
    },
    {
      "id": "reflex_cooldown_gap",
      "name": "Reflex cooldown — 冷却期内无效 escape (adaptive cooldown)",
      "version": "2.0.0",
      "description": "Reflex correctly detects anomaly but fixed cooldown prevents re-trigger during prolonged stuck. Cooldown should adapt to how long the agent has been stuck.",
      "conditions": {
        "anomaly_state_not_idle": true,
        "reflex_active": false,
        "stuck_duration": {"min": 60}
      },
      "diagnosis": "Fixed reflex cooldown (10s) prevents re-trigger during prolonged stuck; cooldown should be adaptive — scale inversely with stuck_duration so that longer stuck periods allow more frequent reflex attempts.",
      "fix_template": "# Fix: Make reflex cooldown adaptive based on stuck_duration\n# File: fly64/fly64/memory.py\n# In ReflexController.__init__: keep base_cooldown_duration=10.0\n# In _start_reflex: replace fixed cooldown with adaptive formula:\n#   cooldown = max(2.0, self.base_cooldown_duration - stuck_duration * 0.05)\n# This ensures: at 60s stuck → cooldown=7.0s, at 120s → cooldown=4.0s, at 160s+ → cooldown=2.0s (minimum)\n# The stuck_duration value is passed in from the anomaly_state context.\n",
      "fix_files": ["fly64/fly64/memory.py"],
      "severity": "medium",
      "tags": ["reflex", "cooldown", "escape", "adaptive"],
      "rollback_strategy": "revert_value",
      "threshold_justification": "anomaly_state_not_idle=anomaly active, reflex_active=False=cooldown period, stuck>60s=1min without effective escape; adaptive cooldown formula derived from stuck_duration to allow more frequent reflex firings as stuck persists"
    },
    {
      "id": "low_coverage_stagnation",
      "name": "Coverage stagnation — 覆盖率长期不增长",
      "version": "1.0.0",
      "description": "Mario trapped in small area; exploration strategy insufficient.",
      "conditions": {
        "coverage_stagnant_120s": true,
        "visited_cells": {"max": 50}
      },
      "diagnosis": "Mario trapped in small area; exploration strategy insufficient.",
      "fix_template": "# Fix: Trigger forced_bold_explore immediately when coverage stagnant\n# File: fly64/fly64/main.py\n# Add before normal escape:\nif coverage_stagnant_120s and visited_cells < 50:\n    forced_bold_explore = True\n",
      "fix_files": ["fly64/fly64/main.py"],
      "severity": "medium",
      "tags": ["exploration", "coverage", "stagnation"],
      "rollback_strategy": "revert_added_block",
      "threshold_justification": "coverage_stagnant_120s=no new cells in 2min, visited_cells<50=very small explored area"
    },
    {
      "id": "below_ground_stuck",
      "name": "Below ground stuck — Y坐标异常导致无法移动",
      "version": "1.0.0",
      "description": "Mario is below normal ground level (Y<50) but above the -100 fallen threshold, so fallen anomaly doesn't trigger. Mario cannot move properly at this height and sits idle.",
      "conditions": {
        "pos_y": {"min": -99, "max": 49},
        "stuck_duration": {"min": 30},
        "control_magnitude": {"max": 10}
      },
      "contradiction": "pos_y=-35 is clearly below ground (normal=120) but fallen threshold is Y<-100",
      "diagnosis": "Fallen detection threshold (Y<-100) is too permissive. SM64 normal ground is Y=120. Y<50 already indicates below-ground (void/water). Fix: change fallen threshold from Y<-100 to Y<50.",
      "fix_template": "# Fix: Lower fallen detection threshold from -100 to 50\n# File: fly64/fly64/memory.py\n# In MotionStateDetector and MemoryController:\n# CHANGE:\n#   fallen = pos_y < -100 or pos_y > 1000\n# TO:\n#   fallen = pos_y < 50 or pos_y > 1000\n# SM64 ground level is Y=120; any Y<50 means Mario is below ground surface.",
      "fix_files": ["fly64/fly64/memory.py"],
      "severity": "high",
      "tags": ["fallen", "y_coordinate", "below_ground", "threshold"],
      "rollback_strategy": "revert_value",
      "threshold_justification": "pos_y between -99 and 49=abnormal height (SM64 ground=120), stuck>30s=stuck in place, control_magnitude<10=no effective movement; the -100 fallen threshold was designed for extreme falls but misses partial falls into void/water at Y=-30 to Y=-80"
    },
    {
      "id": "fallen_recovery_stuck",
      "name": "Fallen recovery stuck — 跌落恢复循环无效",
      "version": "1.0.0",
      "description": "Agent is in fallen anomaly state and recovery cycles (jump+burst+turn) are not making progress. The recovery stuck means the agent repeatedly attempts jump-forward-turn cycles but remains in the fallen state without escaping the fall zone.",
      "conditions": {
        "anomaly_state": {"eq": "fallen"},
        "stuck_duration": {"min": 30}
      },
      "diagnosis": "Fallen recovery cycles are ineffective: always turning -50 (left) fails to find an exit direction. Fix: (1) Mirror turn direction each cycle (alternate left/right), (2) Extend forward burst from 0.8s to 2s with y=80 for more distance, (3) Add reverse-before-jump when stuck>30s to back away from obstacles before the next jump.",
      "fix_template": "# Fix: Fallen recovery — mirror turn, extend burst, add reverse-before-jump\n# File: fly64/fly64/main.py\n# In the escape control section, replace the fallen recovery block:\n# 1. Initialize escape_x on first cycle (if escape_x == 0: escape_x = -50)\n# 2. Use escape_x (alternating sign) instead of hardcoded -50\n# 3. Extend burst phase: elif escape_toggle_timer < 2.4 with y=80\n# 4. In else: flip escape_x = -escape_x for next cycle; if stuck_duration > 30: control.x = -escape_x; control.y = -40 for reverse",
      "fix_files": ["fly64/fly64/main.py"],
      "severity": "high",
      "tags": ["fallen", "recovery", "stuck", "escape", "cycle"],
      "rollback_strategy": "revert_block",
      "threshold_justification": "anomaly_state='fallen'=agent detected in fall zone, stuck_duration>30s=persistent stuck despite recovery attempts; 30s allows ~15 recovery cycles before intervention"
    },
    {
      "id": "suspended_animation",
      "name": "Suspended animation — 控制信号为零停止响应",
      "version": "1.0.0",
      "description": "All control signals are zero (x=0, y=0, jump=False) while stuck_duration is increasing. Mario is completely idle without any escape attempt.",
      "conditions": {
        "control_x_zero": true,
        "control_y_zero": true,
        "stuck_duration": {"min": 15},
        "jump_not_active": true
      },
      "diagnosis": "Brain model is outputting zero control signals while Mario is not in an escape state. This could indicate a visual disconnection, bridge issue, or the model has entered a dead state.",
      "fix_template": "# Investigate: All control signals zero while game state=1 neural control active\n# Check: bridge connectivity, model.visual_connected flag, encode_retina() output\n# Check: timeout recovery in main.py for visual disconnection\n",
      "fix_files": ["fly64/fly64/main.py", "fly64/fly64/model.py"],
      "severity": "high",
      "tags": ["control", "dead_state", "disconnected", "bridge"],
      "rollback_strategy": "investigate",
      "threshold_justification": "control_x/y both zero=no movement, stuck>15s=not just startup transient, jump=False=no escape attempt; combined with seq advancing indicates brain is running but outputting zero"
    },
    {
      "id": "wall_corner_command_decoupled",
      "name": "Wall corner — 视觉盲区：动作-效果失配卡角",
      "version": "1.0.0",
      "description": "Mario wedged in wall corner. Invisible to texture-based vision (static frame, symmetric walls, zero transients) — detected only via motor-vs-motion mismatch (corollary discharge comparator).",
      "conditions": {
        "command_effect_mismatch": true,
        "mismatch_duration": {"min": 15}
      },
      "contradiction": "visual signals (temporal=0, ON/OFF=0, asymmetry=0) are identical to standing still in open field",
      "diagnosis": "Visual causal chain has no action-effect comparator: 'stuck' is defined by commanded vs measured displacement (proprioception), which texture-based vision cannot supply. Fix: corollary-discharge comparator + reverse-out reflex.",
      "fix_template": "# Fix: corollary discharge comparator (action-effect mismatch)\n# File: fly64/fly64/main.py\n# expected = control.y * 0.6; actual = pose displacement\n# mismatch if expected>25 && moved<3 for >15 frames\n# Reflex: reverse y=-50 + alternating turn to un-wedge",
      "fix_files": ["fly64/fly64/main.py"],
      "severity": "high",
      "tags": ["corner", "proprioception", "corollary_discharge", "blind_spot"],
      "rollback_strategy": "revert_added_block",
      "threshold_justification": "expected>25 units/tick = meaningful forward command, moved<3 = static, >15 frames = not transient; reverse+turn physically un-wedges from 90-degree corners where asymmetry guidance is zero"
    },
    {
      "id": "dopamine_plateau",
      "name": "Dopamine gain plateau — Learning saturation detected",
      "version": "1.0.0",
      "description": "Dopamine-gated gain has reached a maximum plateau without further improvement in error gradient. The plasticity proxy may be saturated — suggests exploring a new strategy or resetting gains.",
      "conditions": {
        "dopamine_gain_avg": {"min": 2.0},
        "learning_progress": {"max": 0.05},
        "stuck_duration": {"min": 60}
      },
      "diagnosis": "Dopamine gain saturated at ceiling (>=2.0) while learning_progress (mean abs error gradient) stagnates below 0.05. The gain modulation cannot further improve motor adaptation — consider resetting gains or switching to a new exploration strategy.",
      "fix_template": "# Reset dopamine gains to break plateau\n# File: fly64/fly64/model.py\n# model.dopamine_gain.reset_gains() to restart adaptation",
      "fix_files": ["fly64/fly64/model.py"],
      "severity": "medium",
      "tags": ["plasticity", "dopamine", "plateau", "t4"],
      "rollback_strategy": "revert_value",
      "threshold_justification": "dopamine_gain_avg>=2.0=near ceiling (GAIN_MAX=2.5), learning_progress<0.05=minimal error gradient (converged), stuck>60s=sufficient run time"
    }
  ]
}